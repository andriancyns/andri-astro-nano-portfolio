---
import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import { SITE, getHomeMetadata, SOCIALS } from "@consts";
import { useTranslations, defaultLang, type Language } from "../i18n";

// Auto-detect language from URL or use default
const currentLang: Language = defaultLang;
const t = useTranslations(currentLang);
const HOME = getHomeMetadata(currentLang);

const blog = (await getCollection("blog"))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_POSTS_ON_HOMEPAGE);

const projects = (await getCollection("projects"))
  .filter(project => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_PROJECTS_ON_HOMEPAGE);

---

<script is:inline src="https://analytics.ahrefs.com/analytics.js" data-key="wW4EfvW+aSyPA3huUIMvXQ" async></script>

<PageLayout 
  title={HOME.TITLE} 
  description={HOME.DESCRIPTION} 
  structuredDataType="person"
  keywords="software engineer, backend developer, mobile developer, go, golang, laravel, php, android, flutter, freelancer, indonesia"
  currentLang={currentLang}
>
  <Container>
    <h4 class="animate font-semibold text-black dark:text-white" {...{ 'data-testid': 'heading' }}>
      {t('home.greeting')}
      <span class="text-xl">👋🏻</span>
    </h4>
    <div class="space-y-16">
      <section>
        <article class="space-y-4">
          <p class="animate">
            {t('home.intro')}
          </p>
        </article>
      </section>

      

      <!-- Work Experience -->
      <!-- <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Work Experience
          </h5>
          <Link href="/work">
            See all work
          </Link>
        </div>
        <ul class="flex flex-col space-y-4">
          {work.map(entry => (
            <li>
              <div class="text-sm opacity-75">
                {dateRange(entry.data.dateStart, entry.data.dateEnd)}
              </div>
              <div class="font-semibold text-black dark:text-white">
                {entry.data.company}
              </div>
              <div class="text-sm opacity-75">
                {entry.data.role}
              </div>
              <article>
                <entry.Content />
              </article>
            </li>
          ))}
        </ul>
      </section> -->

      <!-- Projects -->
      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            {t('home.recent_projects')}
          </h5>
          <Link href="/projects">
            {t('home.see_all_projects')}
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {projects.map(project => (
            <li>
              <ArrowCard entry={project} lang={currentLang} />
            </li>
          ))}
        </ul>
      </section>

      <!-- Blog -->
      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            {t('home.latest_posts')}
          </h5>
          <Link href="/blog">
            {t('home.see_all_posts')}
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {blog.map(post => (
            <li>
              <ArrowCard entry={post} lang={currentLang} />
            </li>
          ))}
        </ul>
      </section>

      <!-- Connect -->
      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">
          {t('home.connect')}
        </h5>
        <article>
          <p>
            {t('home.connect_text')}
          </p>
        </article>
        <ul class="flex flex-wrap gap-2">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
              {SITE.EMAIL}
            </Link>
          </li>
          <li class="line-clamp-1">
            <Link href="/files/resume-andriancyns-software-engineer.pdf" download="resume-andriancyns-software-engineer.pdf" aria-label="Download Portfolio">
              {t('home.download_resume')}
            </Link>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
