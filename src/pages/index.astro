---
import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import AvailabilityBadge from "@components/AvailabilityBadge.astro";
import NewsletterForm from "@components/NewsletterForm.astro";
import { SITE, HOME, SOCIALS } from "@consts";

const blog = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_POSTS_ON_HOMEPAGE);

const projects = (await getCollection("projects"))
  .filter((project) => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_PROJECTS_ON_HOMEPAGE);
---

<script
  is:inline
  src="https://analytics.ahrefs.com/analytics.js"
  data-key="wW4EfvW+aSyPA3huUIMvXQ"
  async></script>

<PageLayout
  title={HOME.TITLE}
  description={HOME.DESCRIPTION}
  structuredDataType="person"
  keywords="software engineer, backend developer, mobile developer, go, golang, laravel, php, android, flutter, freelancer, indonesia"
>
  <Container>
    <div class="animate flex flex-wrap items-center gap-x-4 gap-y-2">
      <h4
        class="font-semibold text-black dark:text-white"
        {...{ "data-testid": "heading" }}
      >
        Hi, I'm Andrian
        <span class="text-xl">👋🏻</span>
      </h4>
      <AvailabilityBadge />
    </div>
    <div class="space-y-16 mt-4">
      <section>
        <article class="space-y-4">
          <p class="animate">
            I'm here to build clean, scalable, and production-ready systems. You
            bring the problem, I'll design the solution—from backend to
            frontend, from brief to deployment.
          </p>
        </article>
      </section>

      <!-- Skills/Tech Stack -->
      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">Tech Stack</h5>
        <div class="flex flex-wrap gap-2">
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >Flutter</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >Android</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >Laravel</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >PHP</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >React</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >Astro</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >TailwindCSS</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >TypeScript</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >MySQL</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >Firebase</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >Python</span
          >
          <span
            class="px-2.5 py-1 text-xs rounded-full border border-black/10 dark:border-white/10 hover:border-black/25 dark:hover:border-white/25 hover:bg-black/5 dark:hover:bg-white/5 transition-colors"
            >Git</span
          >
        </div>
      </section>

      <!-- Work Experience -->
      <!-- <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Work Experience
          </h5>
          <Link href="/work">
            See all work
          </Link>
        </div>
        <ul class="flex flex-col space-y-4">
          {work.map(entry => (
            <li>
              <div class="text-sm opacity-75">
                {dateRange(entry.data.dateStart, entry.data.dateEnd)}
              </div>
              <div class="font-semibold text-black dark:text-white">
                {entry.data.company}
              </div>
              <div class="text-sm opacity-75">
                {entry.data.role}
              </div>
              <article>
                <entry.Content />
              </article>
            </li>
          ))}
        </ul>
      </section> -->

      <!-- Projects -->
      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Recent Projects
          </h5>
          <Link href="/projects"> See all projects </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {
            projects.map((project) => (
              <li>
                <ArrowCard entry={project} />
              </li>
            ))
          }
        </ul>
      </section>

      <!-- Blog -->
      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Latest Articles
          </h5>
          <Link href="/blog"> See all articles </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {
            blog.map((post) => (
              <li>
                <ArrowCard entry={post} />
              </li>
            ))
          }
        </ul>
      </section>

      <!-- Newsletter -->
      <section class="animate">
        <NewsletterForm />
      </section>

      <!-- Connect -->
      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">Let's Connect</h5>
        <article>
          <p>
            If you'd like to discuss a project or just say hello, feel free to
            reach out via social media or send me an email.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2">
          {
            SOCIALS.map((SOCIAL) => (
              <li class="flex gap-x-2 text-nowrap">
                <Link
                  href={SOCIAL.HREF}
                  external
                  aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}
                >
                  {SOCIAL.NAME}
                </Link>
                {"/"}
              </li>
            ))
          }
          <li class="line-clamp-1">
            <Link
              href={`mailto:${SITE.EMAIL}`}
              aria-label={`Email ${SITE.NAME}`}
            >
              {SITE.EMAIL}
            </Link>
          </li>
          <li class="line-clamp-1">
            <Link
              href="/files/resume-andriancyns-software-engineer.pdf"
              download="resume-andrianc-full-stack-developer.pdf"
              aria-label="Download Portfolio"
            >
              download my resume
            </Link>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
