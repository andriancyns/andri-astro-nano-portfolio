---
import PageLayout from "@layouts/PageLayout.astro";
import { SITE } from "@consts";

// Digital Products - Edit this array to add/remove products
const products = [
  {
    icon: "📚",
    name: "Flutter Development Handbook",
    description: "Complete guide to building production-ready Flutter apps",
    price: "$19",
    badge: "Popular",
    links: [
      { platform: "Gumroad", url: "https://gumroad.com/", icon: "🛒" },
      { platform: "Etsy", url: "https://etsy.com/", icon: "🏪" },
    ],
  },
  {
    icon: "📝",
    name: "Freelancer Invoice Template",
    description: "Professional Excel/Google Sheets template for freelancers",
    price: "Free",
    links: [{ platform: "Gumroad", url: "https://gumroad.com/", icon: "🛒" }],
  },
  {
    icon: "🎨",
    name: "Developer Portfolio Template",
    description: "Modern Astro template for software developers",
    price: "$9",
    links: [
      { platform: "Gumroad", url: "https://gumroad.com/", icon: "🛒" },
      { platform: "GitHub", url: "https://github.com/", icon: "🐙" },
    ],
  },
  {
    icon: "⚙️",
    name: "Laravel API Boilerplate",
    description: "Production-ready Laravel API starter with auth & docs",
    price: "Free",
    links: [{ platform: "GitHub", url: "https://github.com/", icon: "🐙" }],
  },
  {
    icon: "📱",
    name: "Mobile App UI Kit",
    description: "Figma UI kit for mobile app designers",
    price: "$15",
    links: [
      { platform: "Gumroad", url: "https://gumroad.com/", icon: "🛒" },
      { platform: "Etsy", url: "https://etsy.com/", icon: "🏪" },
      { platform: "Figma", url: "https://figma.com/", icon: "🎨" },
    ],
  },
];

const socials = [
  { name: "GitHub", link: "https://github.com/andriancyns", icon: "🐙" },
  { name: "LinkedIn", link: "https://linkedin.com/in/andriancyns", icon: "💼" },
  { name: "Email", link: "mailto:andriancyns@gmail.com", icon: "📧" },
  { name: "WhatsApp", link: "https://wa.me/62816663942", icon: "💬" },
];
---

<PageLayout
  title="Links | Andrian Cahyono"
  description="Digital products, resources, and social links from Andrian Cahyono"
  keywords="digital products, ebook, template, resources, links"
>
  <div class="min-h-screen py-8 px-4 relative overflow-hidden">
    <!-- Background gradient blobs -->
    <div
      class="absolute top-0 left-0 w-72 h-72 bg-gradient-to-r from-green-500/20 to-blue-500/20 rounded-full blur-3xl -translate-x-1/2 -translate-y-1/2 pointer-events-none"
    >
    </div>
    <div
      class="absolute bottom-0 right-0 w-72 h-72 bg-gradient-to-r from-purple-500/15 to-pink-500/15 rounded-full blur-3xl translate-x-1/2 translate-y-1/2 pointer-events-none"
    >
    </div>

    <div class="max-w-sm mx-auto space-y-5 relative z-10">
      <!-- Profile Header - Compact -->
      <div class="text-center space-y-2 animate glass-card p-5 rounded-xl">
        <div
          class="w-16 h-16 mx-auto rounded-full bg-gradient-to-br from-green-400 to-blue-500 flex items-center justify-center text-2xl shadow-lg shadow-green-500/20"
        >
          👨‍💻
        </div>
        <div>
          <h1 class="text-lg font-bold text-black dark:text-white">
            Andrian Cahyono
          </h1>
          <p class="text-xs opacity-60">Software Engineer • Digital Creator</p>
        </div>
      </div>

      <!-- Digital Products -->
      <div class="space-y-2 animate">
        <h2
          class="text-xs font-medium opacity-40 uppercase tracking-wider text-center"
        >
          Products
        </h2>
        {
          products.map((product, index) => (
            <>
              {/* Single link - entire card is clickable */}
              {product.links.length === 1 ? (
                <a
                  href={product.links[0].url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="product-card product-card-clickable block"
                  style={`animation-delay: ${index * 50}ms`}
                >
                  <div class="flex items-center gap-3 p-3">
                    <div class="w-9 h-9 rounded-lg bg-white/10 flex items-center justify-center text-lg flex-shrink-0">
                      {product.icon}
                    </div>
                    <div class="flex-1 min-w-0">
                      <div class="flex items-center gap-1.5">
                        <span class="text-sm font-medium text-black dark:text-white truncate">
                          {product.name}
                        </span>
                        {product.badge && (
                          <span class="px-1.5 py-0.5 text-[10px] bg-green-500 text-white rounded-full">
                            {product.badge}
                          </span>
                        )}
                      </div>
                      <p class="text-xs opacity-50 truncate">
                        {product.description}
                      </p>
                    </div>
                    <span
                      class={`text-xs font-bold px-2 py-0.5 rounded-full flex-shrink-0 ${
                        product.price === "Free"
                          ? "bg-green-500/20 text-green-500"
                          : "opacity-70"
                      }`}
                    >
                      {product.price}
                    </span>
                    <svg
                      class="w-4 h-4 opacity-40 flex-shrink-0"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M9 5l7 7-7 7"
                      />
                    </svg>
                  </div>
                </a>
              ) : (
                /* Multiple links - show platform buttons */
                <div
                  class="product-card"
                  style={`animation-delay: ${index * 50}ms`}
                >
                  <div class="flex items-center gap-3 p-3">
                    <div class="w-9 h-9 rounded-lg bg-white/10 flex items-center justify-center text-lg flex-shrink-0">
                      {product.icon}
                    </div>
                    <div class="flex-1 min-w-0">
                      <div class="flex items-center gap-1.5">
                        <span class="text-sm font-medium text-black dark:text-white truncate">
                          {product.name}
                        </span>
                        {product.badge && (
                          <span class="px-1.5 py-0.5 text-[10px] bg-green-500 text-white rounded-full">
                            {product.badge}
                          </span>
                        )}
                      </div>
                      <p class="text-xs opacity-50 truncate">
                        {product.description}
                      </p>
                    </div>
                    <span
                      class={`text-xs font-bold px-2 py-0.5 rounded-full flex-shrink-0 ${
                        product.price === "Free"
                          ? "bg-green-500/20 text-green-500"
                          : "opacity-70"
                      }`}
                    >
                      {product.price}
                    </span>
                  </div>
                  <div class="flex gap-1.5 px-3 pb-3">
                    {product.links.map((link) => (
                      <a
                        href={link.url}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="platform-btn"
                      >
                        <span>{link.icon}</span>
                        <span>{link.platform}</span>
                      </a>
                    ))}
                  </div>
                </div>
              )}
            </>
          ))
        }
      </div>

      <!-- Social Links -->
      <div class="space-y-2 animate">
        <h2
          class="text-xs font-medium opacity-40 uppercase tracking-wider text-center"
        >
          Connect
        </h2>
        <div class="grid grid-cols-4 gap-2">
          {
            socials.map((social) => (
              <a
                href={social.link}
                target="_blank"
                rel="noopener noreferrer"
                class="social-btn"
                title={social.name}
              >
                <span class="text-lg">{social.icon}</span>
              </a>
            ))
          }
        </div>
      </div>

      <!-- Back Link -->
      <div class="text-center animate">
        <a
          href="/"
          class="inline-flex items-center gap-1.5 text-xs opacity-50 hover:opacity-100 transition-all"
        >
          <svg
            class="w-3 h-3"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          {SITE.NAME}
        </a>
      </div>
    </div>
  </div>

  <style>
    .glass-card {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.08);
    }

    .product-card {
      border-radius: 0.75rem;
      background: rgba(255, 255, 255, 0.04);
      border: 1px solid rgba(255, 255, 255, 0.08);
      transition: all 0.2s ease;
    }

    .product-card:hover {
      background: rgba(255, 255, 255, 0.06);
      border-color: rgba(255, 255, 255, 0.12);
    }

    .product-card-clickable {
      cursor: pointer;
    }

    .product-card-clickable:hover {
      background: rgba(34, 197, 94, 0.08);
      border-color: rgba(34, 197, 94, 0.25);
      transform: translateY(-1px);
    }

    .platform-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.375rem;
      flex: 1;
      padding: 0.5rem 0.75rem;
      background: rgba(255, 255, 255, 0.06);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 0.5rem;
      font-size: 0.75rem;
      transition: all 0.15s ease;
    }

    .platform-btn:hover {
      background: rgba(34, 197, 94, 0.12);
      border-color: rgba(34, 197, 94, 0.3);
    }

    .social-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0.625rem;
      background: rgba(255, 255, 255, 0.04);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 0.625rem;
      transition: all 0.15s ease;
    }

    .social-btn:hover {
      background: rgba(255, 255, 255, 0.08);
      transform: translateY(-1px);
    }

    :global(.light) .glass-card,
    :global(.light) .product-card,
    :global(.light) .social-btn {
      background: rgba(255, 255, 255, 0.6);
      border-color: rgba(0, 0, 0, 0.08);
    }

    :global(.light) .product-card-clickable:hover {
      background: rgba(34, 197, 94, 0.08);
    }

    :global(.light) .platform-btn {
      background: rgba(0, 0, 0, 0.04);
      border-color: rgba(0, 0, 0, 0.08);
    }

    :global(.light) .platform-btn:hover {
      background: rgba(34, 197, 94, 0.1);
    }
  </style>
</PageLayout>
